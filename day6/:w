#include <string>
#include <fstream>
#include <iostream>

using namespace std;
int main(){
    ifstream reader("input.txt");
    stringstream buffer;
    buffer << reader.rdbuf();
    string str = buffer.str();

    int markerlen = 4;
    for (int i =0 ; i < str.length(); i++) {

    }
}
